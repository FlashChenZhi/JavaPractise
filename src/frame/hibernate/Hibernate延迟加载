                                                        Hibernate延迟加载
一、两种不同的延迟加载
1.属性的延迟加载：load()方法
    当使用load的方式来获取对象的时候，只有访问了该对象的属性，hibernate才会到数据库中进行查询；
    否则不会访问数据库

2.关系的延迟加载：get()方法
    关系延迟加载又叫懒加载，即lazyload，又叫懒汉式或饿汉式
    作用：把对象的创建延迟到使用的时候创建，而不是对象实例化的时候创建。这种方式避免了性能的浪费